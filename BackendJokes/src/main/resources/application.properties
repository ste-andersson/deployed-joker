spring.application.name=BackendJokes

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:saltdb}
spring.datasource.username=${DB_USER:salt}
spring.datasource.password=${DB_PASSWORD:secret}

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update